/*
void setup() {
  size(700, 700, P3D);
}

void draw() {
  background(255);
  translate(width / 2, height / 2, 0);
  rotateY(frameCount / 100.0);
  for (int x = -2; x < 3; ++x) {
    for (int y = -2; y < 3; ++y) {
      for (int z = -2; z < 3; ++z) {
        fill(map(x, -2, 2, 0, 255), map(y, -2, 2, 0, 255), map(z, -2, 2, 0, 255));
        push();
        translate(80 * x, 80 * y, 80 * z);
        box(50);
        pop();
      }
    }
  }
}
*/
